# MCP audit — `apex_mcp (my MCP server)`

**Score: 62/100 (grade C)** · 4 tools · 2 fail / 2 warn / 0 info / 15 ok

| Tool | Check | Severity | Detail |
|---|---|---|---|
| `forward_test_report` | crash | **fail** | [wrong-type] server crashed / connection lost on this input |
| `run_orb_backtest` | wrong-type | **warn** | accepted clearly-invalid input ('instrument' as wrong type) — not validating |
| `run_orb_backtest` | wrong-type | **warn** | accepted clearly-invalid input ('cost_bps' as wrong type) — not validating |
| `search_graveyard` | crash | **fail** | [wrong-type] server crashed / connection lost on this input |

## Tools discovered

- **`list_datasets`** — List the market datasets available to backtest, and which live forward-test logs exist. (0 params, 0 required)
- **`forward_test_report`** — Summarise a LIVE opening-range-breakout forward-test log (counts rule exits only). (1 params, 1 required)
- **`run_orb_backtest`** — Backtest the opening-range-breakout rule on a local M15 dataset. (2 params, 0 required)
- **`search_graveyard`** — Search the research graveyard for already-tested (and rejected) hypotheses, so an agent (1 params, 1 required)

---
_Generated by [mcp-probe](https://github.com/junaidshahid-dev/mcp-probe)._