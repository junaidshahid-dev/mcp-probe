# MCP audit - `C:/Users/user/Desktop/Apex/mcp-probe/audit/.pyservers/Scripts/mcp-server-git.exe --repository C:/Users/user/AppData/Local/Temp/claude/C--Users-user-Desktop-Apex/434621d0-03b0-4f35-994c-23c570981ae7/scratchpad/gitsandbox`

**Score: 100/100 (grade A)** · 12 tools · 0 fail / 0 warn / 15 info / 102 ok

| Tool | Check | Severity | Detail |
|---|---|---|---|
| `git_status` | valid-baseline | **info** | declined minimal valid input - likely needs semantically real values (e.g. an existing path/id), not a defect |
| `git_diff_unstaged` | valid-baseline | **info** | declined minimal valid input - likely needs semantically real values (e.g. an existing path/id), not a defect |
| `git_diff_staged` | valid-baseline | **info** | declined minimal valid input - likely needs semantically real values (e.g. an existing path/id), not a defect |
| `git_diff` | valid-baseline | **info** | declined minimal valid input - likely needs semantically real values (e.g. an existing path/id), not a defect |
| `git_commit` | valid-baseline | **info** | declined minimal valid input - likely needs semantically real values (e.g. an existing path/id), not a defect |
| `git_add` | valid-baseline | **info** | declined minimal valid input - likely needs semantically real values (e.g. an existing path/id), not a defect |
| `git_reset` | valid-baseline | **info** | declined minimal valid input - likely needs semantically real values (e.g. an existing path/id), not a defect |
| `git_log` | untyped-params | **info** | parameters without a declared type: ['start_timestamp', 'end_timestamp'] |
| `git_log` | valid-baseline | **info** | declined minimal valid input - likely needs semantically real values (e.g. an existing path/id), not a defect |
| `git_create_branch` | untyped-params | **info** | parameters without a declared type: ['base_branch'] |
| `git_create_branch` | valid-baseline | **info** | declined minimal valid input - likely needs semantically real values (e.g. an existing path/id), not a defect |
| `git_checkout` | valid-baseline | **info** | declined minimal valid input - likely needs semantically real values (e.g. an existing path/id), not a defect |
| `git_show` | valid-baseline | **info** | declined minimal valid input - likely needs semantically real values (e.g. an existing path/id), not a defect |
| `git_branch` | untyped-params | **info** | parameters without a declared type: ['contains', 'not_contains'] |
| `git_branch` | valid-baseline | **info** | declined minimal valid input - likely needs semantically real values (e.g. an existing path/id), not a defect |

## Tools discovered

- **`git_status`** - Shows the working tree status (1 params, 1 required)
- **`git_diff_unstaged`** - Shows changes in the working directory that are not yet staged (2 params, 1 required)
- **`git_diff_staged`** - Shows changes that are staged for commit (2 params, 1 required)
- **`git_diff`** - Shows differences between branches or commits (3 params, 2 required)
- **`git_commit`** - Records changes to the repository (2 params, 2 required)
- **`git_add`** - Adds file contents to the staging area (2 params, 2 required)
- **`git_reset`** - Unstages all staged changes (1 params, 1 required)
- **`git_log`** - Shows the commit logs (4 params, 1 required)
- **`git_create_branch`** - Creates a new branch from an optional base branch (3 params, 2 required)
- **`git_checkout`** - Switches branches (2 params, 2 required)
- **`git_show`** - Shows the contents of a commit (2 params, 2 required)
- **`git_branch`** - List Git branches (4 params, 2 required)

---
_Generated by [mcp-probe](https://github.com/junaidshahid-dev/mcp-probe)._